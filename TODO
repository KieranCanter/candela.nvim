TODO (before release):
  * when matching returns no results, capture error and vim.notify instead of throwing error
  * consolidate match/find into single functions that acts on either a single, many, or all patterns
    * match/find selected mapped to m/f not M/F
  * give config option to cycle colors constantly or randomly (same colors show first everytime or random selection from palette)
  * improve API naming (make it as user-friendly as possible)
  * implement help function
  * write docs
  * move autocmds/mappings to separate file
  * remove all functions that aren't needed across files from appropriate table e.g. CandelaPatternList.get_next_color() => local get_next_color()

TODO (can wait til after release)
  * when colorscheme changes, if the mode changed (dark -> light or light -> dark) update pattern_list palette and update existing extmarks
  * change_color improvements:
    * set some default names for palette colors so users can do e.g. change_color("red")
    * allow users to set their own color names
    * handle automatic foreground color changing if the colors are too similar in luminosity
  * lazy load more
